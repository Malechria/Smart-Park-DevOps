apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartpark.fullname" . }}
  labels:
    {{- include "smartpark.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smartpark.selectorLabels" . | nindent 4 }}
